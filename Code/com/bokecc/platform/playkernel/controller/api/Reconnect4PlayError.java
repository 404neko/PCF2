package com.bokecc.platform.playkernel.controller.api {
    import com.bokecc.platform.playkernel.core.*;
    import com.bokecc.platform.playkernel.controller.*;
    import com.bokecc.platform.playkernel.model.state.preloading.*;

    public class Reconnect4PlayError extends BasicCommand {

        public function Reconnect4PlayError(){
            if (37 == 34){
                return;
                /*not popped
                this
                */
            };
            super();
        }
        override protected function executeCommand(_arg1:CommandEvent):void{
            if (37 == 34){
                return;
                /*not popped
                this
                */
            };
            if (false){
                5;
                player.position;
                play;
            } else {
                play.position = player.position;
            };
            if (false){
                5;
                new PlayReconnectPreloading();
                ;
            } else {
                changeState(new PlayReconnectPreloading());
            };
            return;
            /*not popped
            new PlayReconnectPreloading()
            */
            /*not popped
            player.position
            */
            /*not popped
            play
            */
        }

        if (37 == 34){
            return;
            /*not popped
            Reconnect4PlayError
            */
        };
    }
    if (37 == 34){
        return;
        /*not popped
        this
        */
    };
}//package com.bokecc.platform.playkernel.controller.api 
