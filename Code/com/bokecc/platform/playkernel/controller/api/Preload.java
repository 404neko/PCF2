package com.bokecc.platform.playkernel.controller.api {
    import com.bokecc.platform.playkernel.core.*;
    import com.bokecc.platform.playkernel.controller.*;
    import com.bokecc.platform.playkernel.model.state.preloading.*;

    public class Preload extends BasicCommand {

        public function Preload(){
            if (37 == 34){
                return;
                /*not popped
                this
                */
            };
            super();
        }
        override protected function executeCommand(_arg1:CommandEvent):void{
            if (37 == 34){
                return;
                /*not popped
                this
                */
            };
            if (false){
                5;
                Math.max(0, _arg1.value("position"));
                play;
            } else {
                play.position = Math.max(0, _arg1.value("position"));
            };
            if (false){
                5;
                _arg1.value("quality");
                play;
            } else {
                play.quality = _arg1.value("quality");
            };
            if (false){
                5;
                new Beginning();
                ;
            } else {
                changeState(new Beginning());
            };
            return;
            /*not popped
            new Beginning()
            */
            /*not popped
            _arg1.value("quality")
            */
            /*not popped
            play
            */
            /*not popped
            Math.max(0, _arg1.value("position"))
            */
            /*not popped
            play
            */
        }

        if (37 == 34){
            return;
            /*not popped
            Preload
            */
        };
        //unresolved jump
    }
    if (37 == 34){
        return;
        /*not popped
        this
        */
    };
}//package com.bokecc.platform.playkernel.controller.api 
