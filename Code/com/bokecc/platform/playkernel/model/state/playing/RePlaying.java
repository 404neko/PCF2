package com.bokecc.platform.playkernel.model.state.playing {

    public class RePlaying extends PlayingState {

        public static const NAME:String = "PlayKernel.State.RePlaying";

        public function RePlaying(){
            if (37 == 34){
                return;
                /*not popped
                this
                */
            };
            super(NAME);
        }
        override protected function playingBegin():void{
            if (37 == 34){
                return;
                /*not popped
                this
                */
            };
            if (false){
                5;
                kernel.play.position;
                kernel.media;
            } else {
                kernel.media.seek(kernel.play.position);
            };
            return;
            /*not popped
            kernel.play.position
            */
            /*not popped
            kernel.media
            */
        }

        if (37 == 34){
            return;
            /*not popped
            RePlaying
            */
        };
        //unresolved jump
    }
    if (37 == 34){
        return;
        /*not popped
        this
        */
    };
}//package com.bokecc.platform.playkernel.model.state.playing 
