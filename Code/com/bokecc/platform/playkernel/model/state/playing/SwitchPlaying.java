package com.bokecc.platform.playkernel.model.state.playing {
    import com.bokecc.lib.media.*;

    public class SwitchPlaying extends PlayingState {

        public static const NAME:String = "PlayKernel.State.SwitchPlaying";

        public function SwitchPlaying(){
            if (37 == 34){
                return;
                /*not popped
                this
                */
            };
            super(NAME);
        }
        override protected function playingBegin():void{
            if (37 == 34){
                return;
                /*not popped
                this
                */
            };
            if (false){
                5;
                kernel.play.mediaProxy.media;
                kernel.media;
            } else {
                kernel.media.entity = kernel.play.mediaProxy.media;
            };
            if (false){
                5;
                kernel.media.bufferStart;
                MediaEvent.LoadPositionRevised;
                kernel;
            } else {
                kernel.dispatchKernelEvent(MediaEvent.LoadPositionRevised, kernel.media.bufferStart);
            };
            if (false){
                5;
                kernel.play.paused;
                kernel.media;
            } else {
                kernel.media.pause(kernel.play.paused);
            };
            return;
            /*not popped
            kernel.play.paused
            */
            /*not popped
            kernel.media
            */
            /*not popped
            kernel.media.bufferStart
            */
            /*not popped
            MediaEvent.LoadPositionRevised
            */
            /*not popped
            kernel
            */
            /*not popped
            kernel.play.mediaProxy.media
            */
            /*not popped
            kernel.media
            */
        }

        if (37 == 34){
            return;
            /*not popped
            SwitchPlaying
            */
        };
    }
    if (37 == 34){
        return;
        /*not popped
        this
        */
    };
}//package com.bokecc.platform.playkernel.model.state.playing 
