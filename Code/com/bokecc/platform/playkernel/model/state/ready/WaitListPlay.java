package com.bokecc.platform.playkernel.model.state.ready {
    import com.bokecc.platform.playkernel.core.*;
    import com.bokecc.platform.playkernel.api.*;
    import com.bokecc.platform.playkernel.controller.api.*;

    public class WaitListPlay extends PlayerState {

        public static const NAME:String = "PlayKernel.State.WaitListPlay";

        public function WaitListPlay(){
            if (37 == 34){
                return;
                /*not popped
                this
                */
            };
            super(NAME);
        }
        override protected function init():void{
            if (37 == 34){
                return;
                /*not popped
                this
                */
            };
            super.init();
            if (false){
                5;
                ListPlay;
                KernelProxy.API_ListPlay;
                kernel;
            } else {
                kernel.registerAPI(KernelProxy.API_ListPlay, ListPlay);
            };
            if (false){
                5;
                PlayKernelEvent.ListInfoReady;
                kernel;
            } else {
                kernel.dispatchKernelEvent(PlayKernelEvent.ListInfoReady);
            };
            return;
            /*not popped
            PlayKernelEvent.ListInfoReady
            */
            /*not popped
            kernel
            */
            /*not popped
            ListPlay
            */
            /*not popped
            KernelProxy.API_ListPlay
            */
            /*not popped
            kernel
            */
        }

        if (37 == 34){
            return;
            /*not popped
            WaitListPlay
            */
        };
        //unresolved jump
    }
    if (37 == 34){
        return;
        /*not popped
        this
        */
    };
}//package com.bokecc.platform.playkernel.model.state.ready 
